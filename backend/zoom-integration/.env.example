# Zoom OAuth App Credentials
# Get these from Zoom App Marketplace -> Your App -> App Credentials
ZOOM_CLIENT_ID=
ZOOM_CLIENT_SECRET=

# The redirect URI registered in your Zoom OAuth app.
# For this standalone server:
#   http://localhost:3000/api/auth/zoom/callback
# For production:
#   https://your-domain.com/api/auth/zoom/callback
ZOOM_REDIRECT_URI=

# Optional: comma-separated list of allowed email domains
# If set, only Firebase users with matching email domains can call the API
# ZOOM_AUTHORIZED_DOMAINS=example.com,acme.org

# Optional: Deep-link scheme for redirecting back to Flutter app after OAuth
# ZOOM_APP_DEEP_LINK=zoomtest://oauth

# Server Configuration
PORT=3000
NODE_ENV=development

# Optional: App version string returned in /api/health
# APP_VERSION=1.0.0

# CORS Configuration (optional)
# CORS_ORIGIN=*

# ============================================
# FIREBASE SETUP:
# ============================================
# This server uses the Firebase Admin SDK.
# It auto-initialises using Application Default Credentials (ADC):
#
# Option A (recommended for local dev):
#   Set GOOGLE_APPLICATION_CREDENTIALS to your service-account JSON path:
#     export GOOGLE_APPLICATION_CREDENTIALS="/path/to/serviceAccountKey.json"
#
# Option B (production / Cloud Run / GCE):
#   The SDK picks up the built-in service account automatically.
#
# Firestore must be enabled in the same Firebase project.
